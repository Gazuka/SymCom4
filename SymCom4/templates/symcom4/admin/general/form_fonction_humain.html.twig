{% extends 'symcom4/admin/template_sb2/_base.html.twig' %}

{% block content %}

    {% embed 'symcom4/admin/template_sb2/elements/_card_with_menu.html.twig' with {'fonction': fonction, 'form': form} only %}
        {% if fonction is defined %}
            {% set titre = "Edition du responsable de la fonction : " ~ fonction.typeFonction.titre %}
        {% else %}
            {% set titre = "Ajout d'un responsable à la fonction" %}
        {% endif %}

        {% set actions = null %}

        {% block contenu %}
            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_row(form.humain) }}
            {{ 'Enregistrer' | BtnSubmitWithIcon('fa-check', 'success') }}
            {{ form_end(form) }}
        {% endblock %}
        
    {% endembed %}

{% endblock %}