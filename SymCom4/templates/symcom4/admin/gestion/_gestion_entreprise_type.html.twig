{% embed 'symcom4/admin/template_sb2/elements/_card_with_menu.html.twig' with {'structure': structure, 'idPage': idPage} only %}
    
    {% set titre = "Type d'entreprise" %}
    
    {% set actions =    {
                            'Editer': path('admin_structures_association_edittype', {'idstructure': structure.id, 'idpagemere': idPage })
                        }
    %}

    {% block contenu %}
        {% if structure.entreprise.types | length == 0 %}
            <p class="text-danger text-center">
                Attention, si l'entreprise n'a pas de type, elle ne sera pas visible sur le site !
            </p>
            <div class='text-center'>{{ path('admin_structures_entreprise_edittype', {'idstructure': structure.id, 'idpagemere': idPage }) | LienBtnWithIcon("Définir le type d'entreprise", 'fa-edit', 'success') }}</div>
        {% else %}
            <ul>
                {% for type in structure.entreprise.types %}
                    <li>{{ type.parentName }}{{ type.nom }}</li>
                {% endfor %}
            </ul>
        {% endif %}
    {% endblock %}
{% endembed %}