{% extends 'symcom4/admin/_text.html.twig' %}
{% block all %}
    <div id="gestion_mails" class="card mb-3"> 
        <div class="card-body">
            <h5 class="card-title">{{ text_service_contact_mail}}</h5>
            {% if mails|length > 0 %}
                <table class="table">
                    <thead class="thead-light">
                        <tr>
                            <th style="width:5%;" scope="col">{{ text_id }}</th>
                            <th style="width:50%;" scope="col">{{ text_service_contact_mail_adresse }}</th>
                            <th style="width:45%;" scope="col">{{ text_actions }}</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for mail in mails %}
                            <tr>
                                <td>{{ mail.id }}</td>
                                <td>{{ mail.adresse }}</td>
                                <td>
                                    {{ path('admin_structure_fonction_editcontact', {'idfonction': parent.id, 'idcontact': mail.contact.id, 'idpage': idPage }) | ModifierBouton }}
                                    {{ path('admin_structure_fonction_deletecontact', {'idfonction': parent.id, 'idcontact': mail.contact.id, 'idpage': idPage}) | SupprimerBouton }}
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table> 
            {% else %}
                <p class="text-danger">{{ text_no_mail }}</p>
            {% endif %}
            {{ path('admin_structure_fonction_addcontact', {'idfonction': parent.id, 'type': 'mail', 'idpage': idPage}) | AddContacts('mail', text_add_mail)}}
        </div>
    </div>
{% endblock %}