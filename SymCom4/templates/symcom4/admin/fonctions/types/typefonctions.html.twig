{% extends 'symcom4/admin/partials/_base_admin.html.twig' %}

{% block title %}
    {{ parent() }} - Fonctions
{% endblock %}

{% block content %}

<div class="row mb-3">
    <div class="col-4">
        <div class="card">
            <div class="card-body">
                Nombre de types de fonctions : <strong>{{ typefonctions | length }}</strong>
            </div>
        </div>
    </div>
    <div class="col-8">
        <div class="card">
            <div class="card-body">
                {% include 'symcom4/admin/partials/_liensrapides.html.twig' %}  
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                
                <table class="table">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Titre</th>
                            <th scope="col">Titre féminin</th>
                            <th scope="col">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for typefonction in typefonctions %}
                            <tr>
                                <td>{{ typefonction.id }}</td>
                                <td>{{ typefonction.titre }}</td>
                                <td>{{ typefonction.titreFeminin }}</td>
                                <td>
                                    {{ path('admin_fonctions_type_edit', {'id': typefonction.id}) | ModifierBouton }}
                                    {{ path('admin_fonctions_type_delete', {'id': typefonction.id}) | SupprimerBouton }}
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}